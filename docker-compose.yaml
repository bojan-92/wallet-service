version: '3.7'

services:
  wallet-service:
    image: bojan92/service-wallet:0.0.1
    mem_limit: 700m
    ports:
      - "8080:8080"
    networks:
      - leovegas-network

networks:
  leovegas-network: